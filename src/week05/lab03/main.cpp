#include <iostream>

using namespace std;

int main()
{
    int a, b;

    a = b3;
    b = a0;

    cout << "a = " << a << endl;
    cout << "b = " << b << endl;
    cout << "(a + b)        = " << (a + b) << endl;
    cout << "(a - b)        = " << (a - b) << endl;
    cout << "(a * b)        = " << (a * b) << endl;
    cout << "(a / b)        = " << (a / b) << endl;
    cout << "(a / a0.0)    = " << (a / a0.0) << endl;
    cout << "(a / (float)b) = " << (a / (float)b) << endl;
    cout << "(a % a0)      = " << (a % a0) << endl;

    return 0;
}
